<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Index test</title>
    <link rel="stylesheet" href="/res/css/main.css?<?= time() ?>">
    <script type="text/javascript" src="/res/js/app.js?<?= time() ?>" defer></script>
</head>
<body>
    <header class="top-menu">
        <?php require __DIR__ . '/top-menu.phtml'; ?>
    </header>

    <container>
        <aside>
            <div class="sidebar-header">
                <button id="hide-sidebar" class="sidebar-icon-button" type="button" title="Hide Categories" aria-label="Hide Categories" aria-pressed="false">
                    <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
                        <path d="M15.41 7.41L14 6l-6 6 6 6 1.41-1.41L10.83 12z" fill="currentColor" />
                    </svg>
                </button>
            </div>
            <div class="category-tree-scroll">
                <?php require __DIR__ . '/category-tree.phtml'; ?>
            </div>
        </aside>

        <main>
            <div class="sort-container">
                <div class="sort-spacer">
                    <button id="show-sidebar" class="sidebar-icon-button sidebar-show-button" type="button" title="Show Categories" aria-label="Show Categories" aria-pressed="false">
                        <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
                            <path d="M8.59 16.59L13.17 12 8.59 7.41 10 6l6 6-6 6z" fill="currentColor" />
                        </svg>
                    </button>
                </div>
                <button id="mobile-categories-toggle" class="mobile-categories-toggle" type="button" aria-label="Показати категорії" aria-expanded="false">
                    Категорії
                </button>
                <div id="pagination-container-top"></div>
                <div class="sort-control">
                    <select id="sort-select">
                        <option value="price">Дешевші</option>
                        <option value="newest">Новіші</option>
                        <option value="a-z">Алфавітний порядок</option>
                    </select>
                </div>
            </div>
            <div id="product-list-container"></div>
            
        </main>
    </container>

    <div id="sidebar-mobile-backdrop" class="sidebar-mobile-backdrop" aria-hidden="true"></div>

    <template id="product-item-template">
        <card class="product-card">
            <h3 class="product-title">{{name}}</h3>
            <img class="product-image" src="{{image_url}}" alt="{{name}}">
            <span class="product-price">{{price}}</span>
            <span class="product-created-at">{{created_at}}</span>
            <button class="product-button" onclick="App.loadProductDetails({{product_id}})">Buy</button>
        </card>
    </template>
    <template id="product-popup-template">
        <div class="product-popup-backdrop"></div>
        <div class="product-popup">
            <div class="product-popup-content"></div>
        </div>
    </template>
    <template id="product-popup-content-template">
        <h3 class="product-title">{{name}}</h3>
        <img class="product-image" src="{{image_url}}" alt="{{name}}">
        <span class="product-price">{{price}}</span>
    </template>
    <template id="product-popup-skeleton-template">
        <div class="product-skeleton product-skeleton-title"></div>
        <div class="product-skeleton product-skeleton-image"></div>
        <div class="product-skeleton product-skeleton-price"></div>
    </template>

    <footer class="page-footer">
        <?php require __DIR__ . '/copyright.phtml'; ?>
    </footer>
</body>
</html>